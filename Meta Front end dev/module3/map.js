// to initialize a map


let beatboxer = new Map()

beatboxer.set(1,"Beast")
beatboxer.set(2,"Ben 10")
beatboxer.set(3,"Dragon ball z")

console.log(beatboxer)

// to target a specific node

console.log(beatboxer.get(2))
// basicallty the firstn parameter is sota the index of the node and the 2nd parameter is the value
